apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqlpad-charts.fullname" . }}
  labels:
    {{- include "sqlpad-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqlpad-charts.selectorLabels" . | nindent 4 }}
